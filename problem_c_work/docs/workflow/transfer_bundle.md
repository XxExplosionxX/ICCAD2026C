Contest-preparation transfer bundle.

Included:
- `problem_c_work/active/` with the current evaluator, solvers, ML scripts, configs, and models
- `problem_c_work/archive/` with milestone history and older evaluator/solver snapshots
- `problem_c_work/artifacts/` with checkpoints, results, and pulled-back local checks
- `problem_c_work/docs/` with workflow notes and milestone reports

Excluded:
- course/school checkpoint PDFs and presentation notes
- dataset archives and downloaded datasets
- local cache artifacts such as `.mplconfig/`, `__pycache__/`, and `*.pyc`
