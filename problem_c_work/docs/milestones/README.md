# Milestone Notes

These notes are preserved as historical records of milestone-by-milestone experiments.

Use `problem_c_work/README.md` for the current workspace structure and canonical commands.

Important:

- `m47` and `ml` notes are the closest to the current active layout.
- Older milestone notes may still reference pre-reorganization paths such as `src/`, `contest/`, `results/`, or `/abs/path/...`.
- Those stale paths should be read as historical context, not as the current canonical file layout.
